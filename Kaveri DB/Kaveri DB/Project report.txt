What I Have Done:

Stage 1 (Data Exploration & Cleaning Proofs):

Handled inconsistent date formats (YYYY-MM-DD, DD/MM/YYYY, and Month DD, YYYY) using regex-based CASE statements.
Identified guest email duplication and normalization rules using LOWER(TRIM(guest_email)).
Isolated data anomalies in legacy records: string formatting in nightly_rate (e.g., '4,500.00'), missing/invalid notes, status text variations, and comma-separated multi-room assignments such as '102,103'.
Caught the room-type conflict where Kaveri Riverside room 103 was mapped to both Deluxe and Standard.

Stage 2 (Schema Design & DDL):

Created custom ENUM types (booking_status, payment_method_type) and enabled the btree_gist extension.
Created normalized target tables: properties, room_types, rooms, guests, rate_plans, bookings, payments, and reviews.
Added constraints: CHECK constraints on ratings and rates, unique indexes on guest emails, unique constraints on property room numbers, and Exclusion Constraints using GiST for non-overlapping booking stays.
Designed an initial ER diagram visualizing table relationships and cardinalities.

Stage 3 (Legacy Migration Execution):

Built unnesting and regex transformation logic to migrate data from legacy_reservations into normalized structures.

What They Have Asked Me to Do:

01_analysis.md: Document Stage 1 findings, key discovery SQL queries, and the explicit decision made regarding the room-type conflict.
02_erd.png: Submit the ER diagram designed prior to revealing the reference schema.
03_schema_original.sql: Export the exact Stage 2 DDL as originally written, without cleaning it up post-reveal.
04_reconciliation.md: Write the comparison interlude: identify matches, document schema differences, and justify any custom design decisions retained over the reference design.
05_schema_final.sql: Save the normalized, renamed schema DDL that all migration and analytical scripts execute against.
06_migration.sql: Write the full ETL migration script that transfers data from legacy_reservations into normalized tables, ending with the zero-discrepancy reconciliation query (3.8).
07_seed.sql: Write SQL insertion scripts for supplementary data (rooms, rate plans, bookings, and reviews) required for Steps 3.9 through 3.12.
08_queries.sql: Provide all 25 Stage 4 SQL queries, properly numbered, with a single-line comment above each stating the target question.
09_break_it.md: Document all Stage 5 constraint violation attempts, exact PostgreSQL error output, caught constraints, and validator output.
10_performance.md: Document Stage 6 performance tuning: record EXPLAIN query plans before and after optimization, list indexes created, and provide the two written analytical arguments.